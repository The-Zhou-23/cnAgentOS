# 手势识别修复文档

## 1. 代码链路梳理

当前项目中的手势识别链路全部位于前端浏览器侧：

1. `app/templates/portal_base.html`
   - 在门户页面统一引入 `app/static/js/gesture.js`
2. `app/static/js/gesture.js`
   - 创建手势开关按钮、摄像头预览视频和状态提示
   - 调用 `navigator.mediaDevices.getUserMedia()` 采集视频流
   - 对视频帧做尺寸归一化、亮度估计和亮度/对比度自适应增强
   - 调用 MediaPipe Hands 做手部关键点检测
   - 基于关键点几何关系提取手势特征并做规则分类
   - 将识别结果做去抖、积分稳定和冷却控制
   - 触发滚动、刷新、返回等页面动作
3. `app/static/css/gesture.css`
   - 负责摄像头浮窗、按钮、状态提示视觉表现

## 2. 根因与修复

### 2.1 识别准确率低 / 部分手势无法识别

- 根因：
  - 原逻辑只用 `tip.y < pip.y` 这类单一纵向规则判定手指是否伸直
  - 拇指判定仅比较腕点距离，倾斜、镜像和遮挡场景极易失真
  - 未区分“手势明显成立”和“多个手势分数接近”的模糊场景
- 修复：
  - 重构特征提取逻辑，增加掌心轴、指尖投影、手指夹角、掌心距离等几何特征
  - 为拇指单独计算展开度、远离掌心程度、竖直方向得分
  - 对 `open_palm`、`fist`、`peace`、`point_up`、`thumbs_up` 建立分数化分类规则
  - 当最佳手势与次优手势分差不足时直接判定为 `unknown`，抑制误触发

### 2.2 识别延迟过高

- 根因：
  - 原实现要求连续 `12` 帧完全一致才触发
  - 原循环未限制并发 `send()`，推理堆积后会带来额外排队延迟
  - 冷却时间过长，导致用户感知“识别不灵敏”
- 修复：
  - 改为基于分数积分的稳定判定，约 2 帧高置信结果即可触发
  - 增加单帧背压控制，同一时刻只允许一个推理请求在途
  - 改为约 `33ms` 的节流采样周期，兼顾时延和 CPU 占用
  - 将冷却时间调整为 `1500ms`

### 2.3 复杂场景下漏检 / 误检频发

- 根因：
  - 原实现无预处理，弱光、强光、逆光场景下输入质量差
  - 未处理多手冲突，第一只手一旦分类不稳就会误判
  - 远距离小手目标仍强行分类，结果不可靠
- 修复：
  - 新增 `256x256` 归一化预处理画布
  - 基于采样亮度自动调整亮度和对比度
  - 允许 `maxNumHands = 2`，并在双手场景下选取最佳候选
  - 多手结果相互冲突且分数接近时直接放弃触发
  - 对掌心尺度过小的远距离目标直接拒绝分类，避免误触发

### 2.4 多手 / 遮挡场景完全失效

- 根因：
  - 原配置 `maxNumHands = 1`
  - 原逻辑永远只取 `results.multiHandLandmarks[0]`
- 修复：
  - 推理配置改为最多 `2` 只手
  - 新增多手候选评分与冲突抑制逻辑

### 2.5 错误处理缺失，功能容易崩溃或“假开启”

- 根因：
  - 摄像头拒绝、脚本缺失、模型超时、推理异常都只写 `console.warn`
  - 资源释放不完整，异常重启和正常停止之间边界不清晰
- 修复：
  - 补充摄像头权限拒绝、浏览器不支持、模型加载失败、推理异常、检测超时、视频流中断的统一降级处理
  - 增加自动重试一次的恢复逻辑，重试失败后自动关闭并撤销启用状态
  - 正常关闭时主动解绑轨道结束事件，避免把正常停止误判成异常中断
  - 新增 `window.cnAgentOS.gesture.getStatus()` 便于运行时排查

## 3. 落地文件

- `app/static/js/gesture.js`
  - 完整重构采集、预处理、检测调度、特征提取、分类稳定器、异常恢复逻辑
- `app/static/css/gesture.css`
  - 补充 `warn` / `error` 状态提示样式，并优化摄像头预览展示

## 4. 当前验证结果

### 4.1 已完成验证

- `gesture.js` 通过 IDE 语法/诊断检查，无新增错误
- `gesture.css` 通过 IDE 语法/诊断检查，无新增错误
- 已从代码层确认以下严重缺陷已被修复：
  - 单手硬编码
  - 无背压导致的推理堆积
  - 粗糙的单维规则分类
  - 关键异常场景无降级处理

### 4.2 待在真实摄像头环境复测的指标

受当前 IDE 会话限制，无法直接在带摄像头浏览器中完成实拍测量，因此以下指标需要在本机浏览器中按第 5 节执行复测：

- 核心常用手势识别准确率是否达到 `>=95%`
- 端到端识别延迟是否达到 `<=100ms`
- 复杂场景漏检率是否达到 `<=3%`
- 是否不存在频繁误触发

## 5. 真实环境复测步骤

1. 启动项目并打开任一门户页
2. 点击手势按钮启用摄像头
3. 打开浏览器控制台，执行：

```javascript
window.cnAgentOS.gesture.getStatus()
```

4. 按以下矩阵逐项测试并记录成功率、平均延迟、漏检和误触发次数

| 维度 | 场景 | 期望结果 |
| --- | --- | --- |
| 光照 | 强光 / 弱光 / 逆光 | 能稳定出结果，无明显抖动 |
| 姿态 | 正视 / 倾斜 / 部分遮挡 / 远距离 | 常用手势可识别，低质量输入不误触发 |
| 多手 | 单手 / 双手同框 / 双手不同手势 | 选取最佳目标，冲突时不误触发 |
| 手势 | open_palm / fist / peace / thumbs_up / point_up | 映射动作准确触发 |
| 稳定性 | 连续操作 3 分钟 | 不崩溃、不明显卡顿、不持续误触发 |

5. 延迟统计建议：
   - 在控制台用 Performance 面板采集一次完整触发过程
   - 从手势稳定出现到页面动作触发的时间差作为端到端延迟

## 6. 建议验收标准

- 常用五类手势单项准确率均不低于 `95%`
- 连续 30 次触发平均延迟不高于 `100ms`
- 强光、弱光、逆光三组场景综合漏检率不高于 `3%`
- 双手冲突或遮挡场景不应出现频繁误触发

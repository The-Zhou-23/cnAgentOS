# 团队任务 2～5 — 五人任务分配（修订版 v2.0）

> **任务来源**：[川农23级团队任务书.pdf](./川农23级团队任务书.pdf)  
> **延续依据**：[团队任务一-任务分配.md](./团队任务一-任务分配.md)、[requirement.txt](./requirement.txt)、[prompt_xyh](./prompt_xyh)  
> **修订说明**：按团队最新分工调整主责边界；**已实现部分**（如智能聊天骨架）归口到对应主责成员继续完善，勿重复造轮子。

---

## 一、任务书分值与主责对照

| 任务编号 | 名称 | **主责成员** | 协作 |
|----------|------|--------------|------|
| 任务二 | 仿微信智能即时通信 | **A** | E（数字员工 @ 接口） |
| 任务三 | 智慧舆情 | **D** | E（问数/瞭望数据）、A（聊天数据） |
| 任务四 | 视觉/语音/自动化增强 | **B** | C（前端嵌入）、全员（接入点） |
| 任务五 | 多数据库支持 | **C** | B（后台配置页权限） |
| 任务一（延续） | 问数 / 数字员工 / 瞭望 / 数据仓库 | **E** | C（UI）、D（舆情用数据） |
| 前端界面优化 | 用户侧 + 管理侧统一体验 | **C** | A、E |
| 汇报展示 | PPT、演示、资料 | **全员** | — |

---

## 二、五人总览

| 成员 | 主责模块 |
|------|----------|
| **A** | **仿微信智能即时通信（任务二全量）** |
| **B** | **视觉 / 语音 / 自动化（任务四）** |
| **C** | **多数据库切换 + 全局前端界面优化** |
| **D** | **智慧舆情（任务三）** |
| **E** | **智能问数、数字员工、智能瞭望、数据仓库** |
| **合计** | — |

### 与任务一角色衔接

| 成员 | 任务一原角色 | 任务 2～5 新定位 |
|------|--------------|------------------|
| A | 认证门户、架构 | 专注 IM，门户首页仅保留聊天入口卡片（改路由不改认证逻辑） |
| B | RBAC、用户后台 | 手势/语音/自动化，**不再**主责聊天后台 |
| C | 智能瞭望 | 瞭望移交 **E**；C 转数据库 + 全站 UI |
| D | 模型、数字员工 | 数字员工移交 **E**；D 转智慧舆情 |
| E | 问数、大屏 | 承接问数+数字员工+瞭望+数仓；大屏可视化并入 **D 舆情** |

---

## 三、协作与路由约定

| 事项 | 负责人 | 说明 |
|------|--------|------|
| `app.py` 路由合并 | **C** | 各成员提交路由表，由 C 统一登记（避免多人直接改冲突） |
| 数据库 `init_db` 片段 | **C** | 新表 DDL 由模块主责人提供 SQL/函数，C 合并进 `db.py` 或调用 `init_schema()` |
| 静态资源目录 | **C** | 全站样式规范；业务 JS/CSS 归各模块主责人 |
| 数字员工 @ 聊天 | **E → A** | E 维护 `DigitalEmployeeRepository.chat_stream()`；A 群聊 @ 只调用不修改实现 |

---

## 四、成员 A — 仿微信智能即时通信

> **范围**：任务书任务二**全部**功能——用户侧 IM + 后台聊天管理 + 与数字员工群聊联动（调用 E 的接口）。

### 4.1 任务书功能清单（A 全权）

| 序号 | 要求 | 状态参考 | 路由/模块 |
|------|------|----------|-----------|
| 1 | 门户可选问数 / 聊天子系统 | 首页已有卡片 | `/`、`/portal/chat` |
| 2 | 1v1 私聊 | 已有骨架 | `ChatRepository` + `portal_chat` |
| 3 | 群聊 | 已有骨架 | 同上 |
| 4 | 通讯录 | 已有 | `contacts` API |
| 5 | 在线查找、加好友（用户名） | 已有 | `search` / `friend` API |
| 6 | 好友删除 / 撤回申请 | 已有 | `unfriend` API |
| 7 | 拉好友建群 | 已有 | `group` API |
| 8 | 文本 / emoji / 文件消息 | 部分完成 | 补全 emoji、文件体验 |
| 9 | 群内 @ 数字员工 | 已有基础 | 调 `DigitalEmployeeRepository` |
| 10 | 拉数字员工入群 | 已有 | 建群勾选数字员工 |
| 11 | 后台群管理（解散/封禁/公告/成员） | 已有 | `/admin/chat/groups` |
| 12 | 后台文件管理（去重存储） | 已有 | `/admin/chat/files` |
| 13 | 后台多服务器管理 | 已有 | `/admin/chat/servers` |
| 14 | 用户端多节点自动切换 | 已有 API | 完善前端自动 failover |
| 15 | 工具与数字员工绑定（后台） | 已有 `admin/tools` | 与 E 联调绑定字段，**勿改** `digital_employee.py` |

### 4.2 独占文件

```
app/models/chat.py
app/controllers/portal_chat.py
app/controllers/admin_chat.py
app/controllers/admin_tools.py          # 聊天侧工具绑定 UI；工具定义在 ai_tool 表，与 E 共表
app/templates/portal/chat.html
app/templates/admin/chat_groups.html
app/templates/admin/chat_files.html
app/templates/admin/chat_servers.html
app/templates/admin/tools.html
app/static/css/chat.css
app/static/js/chat.js
uploads/chat/                           # 聊天文件存储
```

### 4.3 任务清单

| 编号 | 任务 | 验收标准 |
|------|------|----------|
| A2-1 | 消息通道加固（轮询/WebSocket 二选一，文档说明） | 私聊/群聊稳定收发 |
| A2-2 | emoji / 动态表情完善 | ≥10 个本地表情 |
| A2-3 | 文件消息：进度、预览、与后台文件管理联动 | 去重生效 |
| A2-4 | 群 @ 数字员工：流式回复、仅 @ 时触发 | 川农/天气/毒鸡汤及 E 配置的助手均可 @ |
| A2-5 | 聊天后台：群管控、公告、成员列表完善 | 任务书管理项全覆盖 |
| A2-6 | 多服务器：用户端无感切换 + 健康检查 | 主节点故障可切换 |
| A2-7 | 提交路由表给 C，联调 E 的数字员工接口 | 接口文档 1 页 |
| A2-8 | 自测用例 + 演示脚本（加好友→私聊→建群→@助手） | 15 分钟可演示 |

### 4.4 禁止修改（须找主责人）

- `app/models/digital_employee.py`、`portal_digital.py` → **E**
- `app/models/database.py`、全局 `portal.css` 大改 → **C**
- `app/models/rbac.py`、`admin.py` 用户权限 → **B**

---

## 五、成员 B — 视觉 / 语音 / 自动化

### 5.1 任务书要求

1. **手势交互**：≥5 种手势，快捷操作系统功能（自行设计）。  
2. **语音播报**：自然嵌入业务场景（登录、消息、分析完成等）。  
3. **自动化**：定时爬取或工作流（自行设计）。

### 5.2 独占文件

```
app/models/scheduler.py
app/models/workflow.py
app/controllers/admin_automation.py
app/templates/admin/automation.html
app/static/js/gesture.js
app/static/js/voice.js
app/static/js/automation.js
app/static/css/gesture.css
```

### 5.3 任务清单

| 编号 | 任务 | 验收标准 |
|------|------|----------|
| B4-1 | 手势模块（MediaPipe 或 TensorFlow.js **本地**） | ≥5 种手势，有映射表 |
| B4-2 | 门户 / 聊天 / 舆情页接入手势钩子 | 至少 3 个页面可用 |
| B4-3 | 语音播报（Web Speech API）+ 开关设置 | 登录、新消息、报告完成 3 场景 |
| B4-4 | 自动化调度器 + 后台配置页 | cron 可配、可启停 |
| B4-5 | 内置任务：定时触发瞭望采集（调 E 提供的函数） | 到点写日志 |
| B4-6 | 演示脚本 + 路由表提交 C | — |

### 5.4 协作

- **E**：提供 `run_scheduled_collect()` 或瞭望采集入口函数（只读调用）。  
- **C**：手势/语音 UI 与全局样式对齐。

---

## 六、成员 C — 多数据库 + 前端界面优化（任务五 + UI）

### 6.1 任务五：多数据库

| 编号 | 任务 | 验收标准 |
|------|------|----------|
| C5-1 | `DatabaseConfig` 抽象，`get_connection()` 统一入口 | 业务代码不直连 sqlite3 |
| C5-2 | 默认 SQLite，后台可切 MySQL | 配置表单可保存 |
| C5-3 | MySQL 自动建表（与 SQLite schema 一致） | `init_db` 双端可跑 |
| C5-4 | 迁移说明 + `pymysql` 依赖 | README 一节 |

### 6.2 前端界面优化（跨任务）

| 编号 | 任务 | 范围 |
|------|------|------|
| C-UI-1 | 统一用户门户：`portal_base`、首页、登录/注册 | 与 A/E 页面风格一致 |
| C-UI-2 | 统一管理后台：侧栏、表格、弹窗规范 | 与 B/D 新页一致 |
| C-UI-3 | 静态资源检查脚本 / 缺失 dist 提示 | 启动时友好告警 |
| C-UI-4 | 响应式：手机端门户与聊天基本可用 | 断点 900px |

### 6.3 独占文件

```
app/models/database.py
app/controllers/admin_database.py
app/templates/admin/database_settings.html
app/static/css/portal.css              # 全局门户样式牵头
app/static/css/admin.css               # 全局后台样式牵头
app.py                                 # 路由合并收口（主责）
scripts/migrate_sqlite_to_mysql.py     # 可选
```

### 6.4 禁止修改

- `chat.py`、`portal_chat.py` → **A**  
- `sentiment.py`、`bigscreen` 相关 → **D**  
- `digital_employee.py`、`watchtower.py` → **E**

---

## 七、成员 D — 智慧舆情

### 7.1 任务书要求

- **数智大屏**：3D 地球、ECharts-GL、词云、数据统计。  
- **智能舆情**：AI 分析**聊天 + 瞭望**数据，输出风险/趋势报告。

### 7.2 独占文件

```
app/models/sentiment.py
app/models/analytics.py                  # 舆情统计（若与 E 数仓重叠，只读 E 的聚合 API）
app/controllers/portal_sentiment.py
app/controllers/admin_sentiment.py       # 可选
app/templates/portal/bigscreen.html
app/templates/portal/sentiment.html
app/static/dist/echarts/                 # 本地 ECharts + GL + wordcloud
app/static/js/bigscreen.js
app/static/js/sentiment.js
```

### 7.3 任务清单

| 编号 | 任务 | 验收标准 |
|------|------|----------|
| D3-1 | 本地 ECharts + ECharts-GL + 词云 | 离线可运行 |
| D3-2 | 3D 地球 + 指标卡 + 词云（聊天/瞭望切换） | 大屏全屏演示 |
| D3-3 | 舆情 AI 分析页（调模型，prompt 模板） | 摘要/风险/趋势结构化输出 |
| D3-4 | 门户入口「智慧舆情」 | 首页或顶栏可进 |
| D3-5 | 对接 E：瞭望词频 API；对接 A：聊天抽样 API | 函数签名冻结 |
| D3-6 | 路由表提交 C + 演示 | — |

### 7.4 数据接口（由 E、A 提供，D 只读）

| 接口 | 提供人 |
|------|--------|
| `WatchtowerRepository.aggregate_keywords(limit)` | E |
| `ChatRepository.sample_messages(limit, since)` | A |
| `ModelServiceRepository.chat_once(prompt)` | 只读调用现有模型层（不修改 D 独占 model 文件，通过 E 或公共封装） |

> **说明**：模型调用若原属任务一 D，现模型维护可约定 **E 牵头模型业务** 或保持 `model_service.py` 为公共层——**D 仅调用、不修改** `model_service.py` 内部实现；若需改 prompt 策略，提交 PR 给 E 评审。

---

## 八、成员 E — 智能问数 / 数字员工 / 瞭望 / 数据仓库

> **范围**：任务一尚未完成的核心业务子系统 + 为任务二/三提供数据与 AI 能力。

### 8.1 模块与需求对照

| 模块 | 需求编号 | 要点 |
|------|----------|------|
| 智能问数 | REQ-F-003 | 用户侧 `/portal/query`，自然语言问数 |
| 数字员工 | REQ-F-007/008 | 管理端 CRUD + 用户大厅 + `chat_stream` 供 A @ |
| 智能瞭望 | REQ-F-009/011 | 采集（可已有）+ 用户浏览 `/portal/watch` |
| 数据仓库 | REQ-F-010 | 抽象层、供问数/舆情取数 |

### 8.2 独占文件

```
app/models/data_warehouse.py             # 新建
app/models/watchtower.py               # 主责（自 C 移交）
app/models/digital_employee.py
app/models/model_service.py            # 模型引擎（与 D 调用约定见上）
app/models/api_interface.py
app/models/ai_tool.py                  # 工具定义；A 的 admin/tools 做绑定 UI
app/controllers/portal_query.py        # 新建
app/controllers/portal_watch.py
app/controllers/portal_digital.py
app/controllers/admin_watch.py
app/controllers/admin_ai.py
app/templates/portal/query.html
app/templates/portal/watch_*.html
app/templates/portal/digital_employee.html
app/templates/admin/watch_*.html
app/templates/admin/digital_employees.html
app/templates/admin/api_interfaces.html
app/templates/admin/models.html
app/static/js/digital_employee.js
app/static/js/query.js
app/static/js/watch.js
```

### 8.3 任务清单

| 编号 | 任务 | 验收标准 |
|------|------|----------|
| E1-1 | 智能问数页 + 对接数据仓库/模型 | `/portal/query` 可问可答 |
| E1-2 | 数字员工：五类助手 + 多轮 + Token 展示 | 大厅与 SSE 稳定 |
| E1-3 | 川农/天气/毒鸡汤助手完善，导出 `chat_stream` 文档 | A 群聊 @ 可用 |
| E1-4 | 智能瞭望用户侧 + 采集联调 | `/portal/watch` |
| E1-5 | 数据仓库：表设计 + 问数/舆情聚合 API | D3-5、E1-1 可调用 |
| E1-6 | 工具集 CRUD（`ai_tool`），与 A 绑定页联调 | 绑定写入 `config_json` |

### 8.4 与 A 的接口（必须冻结）

```python
# DigitalEmployeeRepository.chat_stream(message: str, history: list[dict] | None = None) -> Iterator[str]
# 消息格式：@别名 问题内容
```

---

## 九、文件归属矩阵（修订）

| 路径 | 负责人 | 他人 |
|------|--------|------|
| `app/models/chat.py` | **A** | E 只读消息抽样 API |
| `app/controllers/portal_chat.py` | **A** | — |
| `app/controllers/admin_chat.py` | **A** | — |
| `app/models/database.py` | **C** | 只读 |
| `app/static/css/portal.css`、`admin.css` | **C** 牵头 | 各模块小改需 PR |
| `app/static/js/gesture.js`、`voice.js` | **B** | — |
| `app/models/sentiment.py` | **D** | — |
| `app/models/watchtower.py` | **E** | D 只读聚合 |
| `app/models/digital_employee.py` | **E** | A 只读 `chat_stream` |
| `app/models/data_warehouse.py` | **E** | D 只读 |
| `app/controllers/portal_query.py` | **E** | — |
| `app.py` | **C** 合并 | 提交 PR |

---

## 十、路由冻结表（提交给成员 C）

| 方法 | 路径 | 负责人 |
|------|------|--------|
| GET | `/portal/chat` | A |
| GET/POST | `/portal/chat/api/*` | A |
| POST | `/portal/chat/upload` | A |
| GET | `/admin/chat/groups` 等 | A |
| GET | `/portal/query` | E |
| GET | `/portal/watch` | E |
| GET | `/portal/digital-employee` | E |
| POST | `/portal/digital-employee/chat` | E |
| GET | `/portal/bigscreen` | D |
| GET | `/portal/sentiment` | D |
| GET/POST | `/admin/database` | C |
| GET/POST | `/admin/automation` | B |

---

## 十一、成员填写区

| 学号 | 姓名 | 角色 | 主责任务 |
|------|------|------|----------|
| 202308325 | 谢亦航 | 成员 A | 任务二 IM 全量 |
| 202308324 | 周子琢 | 成员 B | 任务四 视觉/语音/自动化 |
| 202308323 | 郝帅立 | 成员 C | 任务五 多库 + UI + app.py 收口 |
| 202308321 | 方乐 | 成员 D | 任务三 智慧舆情 |
| 202308322 | 赵洲 | 成员 E | 问数+数字员工+瞭望+数仓 |

---

## 十二、风险与缓冲

| 风险 | 对策 |
|------|------|
| A 任务二过重 | 已有聊天骨架；优先 MVP，表情/文件可二期 |
| E 模块多 | 问数与数仓先做 API，界面可迭代 |
| D 依赖 E/A 数据 | 第 2 周冻结抽样 API，先用 mock |
| 多人改 `app.py` | **仅 C** 合并路由 |
| 数字员工与聊天 @ | E 先冻结 `chat_stream`，A 后接入 |

---

*文档版本：v2.0 | 修订日期：2026-05-26 | 替代 v1.0 五人交叉分工方案*

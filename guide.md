# 团队协作开发指南

本文档用于指导团队使用 Git 和 GitHub 对本项目进行协作开发、功能迭代和旧代码维护，帮助大家保持代码统一、流程清晰、交付稳定。

## 1. 协作目标

- 规范多人协作流程，避免代码冲突和重复开发。
- 保持主分支稳定可用，减少回滚和事故风险。
- 新功能开发与旧代码维护并行推进，保证可持续迭代。
- 统一代码风格、提交规范和评审标准，提升项目可维护性。

## 2. 推荐分支策略

建议使用以下分支模型：

- `main`：主分支，保持随时可发布状态。
- `dev`：开发集成分支，团队日常合并的主要分支。
- `feature/<功能名>`：新功能分支，例如 `feature/user-permission`。
- `fix/<问题名>`：缺陷修复分支，例如 `fix/login-error`。
- `refactor/<重构名>`：重构分支，例如 `refactor/admin-layout`。
- `hotfix/<紧急修复>`：紧急线上修复分支。

### 分支使用原则

1. 不直接在 `main` 上开发。
2. 每个需求或缺陷尽量对应一个独立分支。
3. 分支名称尽量简洁、可读、可追踪。
4. 功能完成后通过 Pull Request 合并到 `dev`，测试通过后再合并到 `main`。

## 3. 标准协作流程

### 3.1 开始新任务

1. 从最新的 `dev` 拉取代码。
2. 新建自己的任务分支。
3. 明确任务范围，确认是否会影响数据库、接口、前端模板或权限逻辑。
4. 如有设计讨论，先在 Issue 或群组中确认方案，再开始编码。

示例：

```bash
git checkout dev
git pull origin dev
git checkout -b feature/new-dashboard
```

### 3.2 开发过程中

- 小步提交，避免一次提交过大。
- 每完成一个可独立验证的步骤就提交一次。
- 开发过程中及时同步 `dev` 分支，降低后续冲突风险。
- 修改公共模块时要特别注意兼容性，避免影响现有功能。

建议频繁执行：

```bash
git fetch origin
git rebase origin/dev
```

如果团队更习惯 merge，也可以统一采用：

```bash
git pull origin dev
```

但团队内最好保持一致，避免历史记录混乱。

### 3.3 提交代码

提交信息建议遵循统一格式，便于追踪和回溯。

推荐格式：

- `feat: 新增用户角色批量管理`
- `fix: 修复登录校验失败问题`
- `refactor: 重构权限判断逻辑`
- `docs: 更新接口说明`
- `test: 补充模型服务测试`
- `chore: 更新依赖配置`

建议每次提交只做一类变更，避免将功能、修复和格式调整混在一起。

### 3.4 发起 Pull Request

在 GitHub 上创建 PR 时，建议包含以下内容：

- 变更背景：为什么要改。
- 主要修改点：做了哪些改动。
- 影响范围：涉及哪些模块、接口、数据库表或页面。
- 测试结果：本地如何验证。
- 风险提示：是否有兼容性风险。

PR 标题建议简洁明确，例如：

- `feat: add watch source batch operations`
- `fix: handle admin user delete edge case`

## 4. 代码评审规范

### 4.1 评审关注点

评审时重点检查：

- 功能是否满足需求。
- 是否影响现有接口、页面、权限和数据结构。
- 是否有明显的安全问题，例如权限绕过、注入风险、敏感信息泄露。
- 是否有重复代码、过度耦合或可读性差的问题。
- 是否补充了必要的注释、测试和文档。

### 4.2 合并前要求

- 代码通过本地验证。
- PR 至少经过一名同事审核。
- 冲突已解决。
- 如涉及数据库变更，已提供迁移方案或初始化说明。
- 如涉及接口变更，已同步前端或调用方。

## 5. 新功能开发规范

新增功能时建议遵循以下步骤：

1. 先梳理需求与影响范围。
2. 确认是否需要新增路由、控制器、模板、数据库字段或权限点。
3. 先实现核心逻辑，再补充页面、校验和错误处理。
4. 添加必要的测试或手工验证步骤。
5. 更新相关文档，确保其他成员可以快速接入。

### 建议的检查清单

- 是否需要新增配置项？
- 是否需要数据库迁移？
- 是否需要调整权限判断？
- 是否需要兼容旧数据？
- 是否会影响已有页面的交互？
- 是否需要补充日志和异常提示？

## 6. 维护旧代码的原则

项目在持续迭代过程中，旧代码维护尤其重要。建议遵循以下原则：

- 优先保证现有功能稳定，再做结构优化。
- 重构要小步推进，不要一次性大面积修改。
- 先补充测试，再改动核心逻辑。
- 对关键路径代码，如登录、权限、数据库操作、模型调用等，修改时要格外谨慎。
- 删除代码前确认是否还有其他模块依赖。

### 旧代码改造建议

- 先抽离重复逻辑，再做结构优化。
- 先兼容旧接口，再逐步切换到新实现。
- 大型重构应拆分成多个 PR，降低审查和回归成本。
- 对历史遗留问题，可先记录 Issue，再按优先级逐步处理。

## 7. 冲突处理规范

多人协作中，冲突不可避免。处理原则如下：

1. 先确认冲突文件的业务含义，不要机械选择一边。
2. 如果冲突涉及逻辑分支、权限或数据处理，必须人工复核。
3. 解决冲突后重新运行相关测试或手工验证。
4. 若冲突范围较大，建议在 PR 中说明处理方式。

### 降低冲突的建议

- 避免多人同时修改同一文件同一区域。
- 在开始大改动前先沟通。
- 定期同步主开发分支。
- 及时合并小改动，减少长期分支漂移。

## 8. GitHub 协作建议

### Issue 管理

- 所有需求、缺陷、优化建议尽量先建 Issue。
- 给 Issue 打上合适标签，例如 `feature`、`bug`、`refactor`、`urgent`。
- 明确负责人、优先级和截止时间。

### Pull Request 管理

- 一个 PR 尽量只解决一个主题。
- PR 描述要写清楚背景和测试方式。
- 评审意见应尽量具体，避免只写“有问题”而不说明原因。
- 合并后及时关闭关联 Issue。

### 版本发布建议

- 重要版本建议打 Tag。
- 发布说明中列出新增、修复和破坏性变更。
- 涉及数据库变更时，必须同步发布说明和部署步骤。

## 9. 测试与验证

每次合并前，至少完成以下验证：

- 启动项目无报错。
- 核心页面可以正常访问。
- 登录、退出、权限控制正常。
- 新增功能可按预期使用。
- 旧功能未被破坏。

如果条件允许，建议补充：

- 单元测试
- 接口测试
- 回归测试
- 权限测试
- 边界条件测试

## 10. 安全与保密要求

协作开发中请注意：

- 不要把密码、密钥、数据库账号等敏感信息提交到仓库。
- 生产环境配置应使用环境变量或独立配置文件管理。
- 对用户输入要做校验和清理，避免注入和跨站风险。
- 上传文件、调用外部接口、执行系统命令等高风险操作必须额外审查。

## 11. 推荐的日常工作习惯

- 每天开始工作前先 `pull` 最新代码。
- 每次提交前先检查变更是否完整。
- 保持提交信息清晰统一。
- 及时清理废弃分支。
- 遇到复杂问题先在 Issue 中讨论，再动手修改。

## 12. 项目协作约定示例

团队可以根据需要进一步约定：

- PR 至少 1 人审核通过后才能合并。
- 影响数据库结构的改动必须附带迁移说明。
- 涉及权限、登录、支付、删除等高风险功能，必须通过专项测试。
- 大于一定规模的重构需要提前公告。
- 版本发布前执行统一回归检查。

## 13. 建议的目录协作方式

当项目继续扩展时，可以保持模块化思路：

- 控制器只负责请求处理。
- 业务逻辑集中在服务层或仓库层。
- 数据访问层统一管理数据库操作。
- 模板和静态资源尽量保持组件化和复用。

这样可以让新成员更快理解项目结构，也更方便团队并行开发。

## 14. 结语

良好的协作流程比单次编码速度更重要。通过统一分支管理、规范提交、认真评审和持续测试，可以让项目在多人参与的情况下依然保持稳定、可维护和易扩展。

如果团队成员持续遵守本指南，新功能开发会更顺畅，旧代码维护也会更安全。
